// Code generated by `ubx sdk gen` from datadog_sloresponse. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Sloresponse_Data_Creator {
  /** Email of the creator. */
  email?: string | Computed<string>;
  /** Handle of the creator. */
  handle?: string | Computed<string>;
  /** Name of the creator. */
  name?: string | Computed<string>;
}

export interface Sloresponse_Data_Query {
  /** A Datadog metric query for total (valid) events. */
  denominator: string | Computed<string>;
  /** A Datadog metric query for good events. */
  numerator: string | Computed<string>;
}

export interface Sloresponse_Data_SliSpecification_Count_BadEventsFormula {
  /** The formula string used to define the count of bad events for the SLI, such as a Datadog query or mathematical expression. (AI-inferred) */
  formula?: string | Computed<string>;
}

export interface Sloresponse_Data_SliSpecification_Count_Queries {
  /** The aggregation method used to combine query results into a single value. Allowed values are `avg`, `sum`, `min`, `max`, `last`, and `count`. (AI-inferred) */
  aggregator?: string | Computed<string>;
  /** A list of organization UUIDs from which to include data in the SLI query, enabling cross-organization SLOs. If omitted, the query runs against the current organization only. (AI-inferred) */
  crossOrgUuids?: string[] | Computed<string[]>;
  dataSource?: string | Computed<string>;
  name?: string | Computed<string>;
  /** The query string for a count query used in the SLI specification. (AI-inferred) */
  query?: string | Computed<string>;
  /** The semantic_mode field specifies how the query result should be interpreted for a count query in a Datadog SLO. It can be set to 'count' for event counts or 'total' for summed numeric values, affecting the SLI calculation. (AI-inferred) */
  semanticMode?: string | Computed<string>;
}

export interface Sloresponse_Data_SliSpecification_Count {
  badEventsFormula?: Sloresponse_Data_SliSpecification_Count_BadEventsFormula | Computed<Sloresponse_Data_SliSpecification_Count_BadEventsFormula>;
  /** Block defining a formula used to compute the good events count for a count-based SLI specification. This is used in place of a simple query when good events are derived from a formula. (AI-inferred) */
  goodEventsFormula?: Sloresponse_Data_SliSpecification_Count_BadEventsFormula | Computed<Sloresponse_Data_SliSpecification_Count_BadEventsFormula>;
  /** List of query objects that define the metric queries for a count-based SLI specification. Each query includes the query string and formula used to calculate the count of events for the SLO. (AI-inferred) */
  queries?: Sloresponse_Data_SliSpecification_Count_Queries[] | Computed<Sloresponse_Data_SliSpecification_Count_Queries[]>;
  /** The formula used to calculate the total number of events for a count-based Service Level Indicator (SLI). It defines a query or formula that supplies the total events count for SLO computations. (AI-inferred) */
  totalEventsFormula?: Sloresponse_Data_SliSpecification_Count_BadEventsFormula | Computed<Sloresponse_Data_SliSpecification_Count_BadEventsFormula>;
}

export interface Sloresponse_Data_SliSpecification_TimeSlice_Query {
  /** List of formulas that compute the SLI from the time slice query's individual queries, using arithmetic expressions. (AI-inferred) */
  formulas?: Sloresponse_Data_SliSpecification_Count_BadEventsFormula[] | Computed<Sloresponse_Data_SliSpecification_Count_BadEventsFormula[]>;
  queries?: Sloresponse_Data_SliSpecification_Count_Queries[] | Computed<Sloresponse_Data_SliSpecification_Count_Queries[]>;
}

export interface Sloresponse_Data_SliSpecification_TimeSlice {
  /** The comparison operator used to evaluate the time slice query against the threshold. Valid values are `>` (greater than), `>=` (greater than or equal), `<` (less than), and `<=` (less than or equal). (AI-inferred) */
  comparator?: string | Computed<string>;
  /** The query object that defines the metric or formula used to calculate the SLI for this time slice. (AI-inferred) */
  query?: Sloresponse_Data_SliSpecification_TimeSlice_Query | Computed<Sloresponse_Data_SliSpecification_TimeSlice_Query>;
  /** The interval, in seconds, at which query data is aggregated for the time-slice SLO. (AI-inferred) */
  queryIntervalSeconds?: number | Computed<number>;
  /** The threshold value that the SLI time slice query result is compared against to determine whether the slice meets the SLO. (AI-inferred) */
  threshold?: number | Computed<number>;
}

export interface Sloresponse_Data_SliSpecification {
  count?: Sloresponse_Data_SliSpecification_Count | Computed<Sloresponse_Data_SliSpecification_Count>;
  /** The time-slice configuration for the SLI, which defines the query and threshold used to evaluate each time slice for the SLO. (AI-inferred) */
  timeSlice?: Sloresponse_Data_SliSpecification_TimeSlice | Computed<Sloresponse_Data_SliSpecification_TimeSlice>;
}

export interface Sloresponse_Data_Thresholds {
  /** The target percentage for the SLO threshold, e.g., 99.9. (AI-inferred) */
  target?: number | Computed<number>;
  /** The human-readable display string for the target value of the SLO threshold, e.g., '99.9%'. (AI-inferred) */
  targetDisplay?: string | Computed<string>;
  /** The rolling time window over which the SLO target is evaluated. Allowed values are 7d, 30d, 90d, or custom. (AI-inferred) */
  timeframe?: string | Computed<string>;
  /** The warning threshold value for the SLO, typically expressed as a percentage (e.g., 99.9). This is the value at which the SLO is considered to be in a warning state. (AI-inferred) */
  warning?: number | Computed<number>;
  warningDisplay?: string | Computed<string>;
}

export interface Sloresponse_Data {
  /** A list of SLO monitors IDs that reference this SLO. This field is returned only when `with_configured_alert_ids` parameter is true in query. */
  configuredAlertIds?: number[] | Computed<number[]>;
  /** Creation timestamp (UNIX time in seconds) Always included in service level objective responses. */
  createdAt?: number | Computed<number>;
  /** Object describing the creator of the shared element. */
  creator?: Sloresponse_Data_Creator | Computed<Sloresponse_Data_Creator>;
  /** A user-defined description of the service level objective. Always included in service level objective responses (but may be `null`). Optional in create/update requests. */
  description?: string | Computed<string>;
  /** A list of (up to 20) monitor groups that narrow the scope of a monitor service level objective. Included in service level objective responses if it is not empty. Optional in create/update requests for monitor service level objectives, but may only be used when then length of the `monitor_ids` field is one. */
  groups?: string[] | Computed<string[]>;
  /** A unique identifier for the service level objective object. Always included in service level objective responses. */
  id?: string | Computed<string>;
  /** Modification timestamp (UNIX time in seconds) Always included in service level objective responses. */
  modifiedAt?: number | Computed<number>;
  /** A list of monitor ids that defines the scope of a monitor service level objective. **Required if type is `monitor`**. */
  monitorIds?: number[] | Computed<number[]>;
  /** The union of monitor tags for all monitors referenced by the `monitor_ids` field. Always included in service level objective responses for monitor service level objectives (but may be empty). Ignored in create/update requests. Does not affect which monitors are included in the service level objective (that is determined entirely by the `monitor_ids` field). */
  monitorTags?: string[] | Computed<string[]>;
  /** The name of the service level objective object. */
  name?: string | Computed<string>;
  /** A count-based (metric) SLO query. This field is superseded by `sli_specification` but is retained for backwards compatibility. Note that Datadog only allows the sum by aggregator to be used because this will sum up all request counts instead of averaging them, or taking the max or min of all of those requests. */
  query?: Sloresponse_Data_Query | Computed<Sloresponse_Data_Query>;
  /** A generic SLI specification. This is used for time-slice and count-based (metric) SLOs only. */
  sliSpecification?: Sloresponse_Data_SliSpecification | Computed<Sloresponse_Data_SliSpecification>;
  /** A list of tags associated with this service level objective. Always included in service level objective responses (but may be empty). Optional in create/update requests. */
  tags?: string[] | Computed<string[]>;
  /** The target threshold such that when the service level indicator is above this threshold over the given timeframe, the objective is being met. */
  targetThreshold?: number | Computed<number>;
  /** The thresholds (timeframes and associated targets) for this service level objective object. */
  thresholds?: Sloresponse_Data_Thresholds[] | Computed<Sloresponse_Data_Thresholds[]>;
  /** The SLO time window options. Note that "custom" is not a valid option for creating or updating SLOs. It is only used when querying SLO history over custom timeframes. */
  timeframe?: string | Computed<string>;
  /** The type of the service level objective. */
  type?: string | Computed<string>;
  /** The optional warning threshold such that when the service level indicator is below this value for the given threshold, but above the target threshold, the objective appears in a "warning" state. This value must be greater than the target threshold. */
  warningThreshold?: number | Computed<number>;
}

const Sloresponse_Data_QueryFields: FieldMap = {
  denominator: "denominator",
  numerator: "numerator",
};

const Sloresponse_Data_SliSpecification_Count_BadEventsFormulaFields: FieldMap = {
  formula: "formula",
};

const Sloresponse_Data_SliSpecification_Count_QueriesFields: FieldMap = {
  aggregator: "aggregator",
  crossOrgUuids: "cross_org_uuids",
  dataSource: "data_source",
  name: "name",
  query: "query",
  semanticMode: "semantic_mode",
};

const Sloresponse_Data_SliSpecification_CountFields: FieldMap = {
  badEventsFormula: {
    wireName: "bad_events_formula",
    kind: "object",
    fields: Sloresponse_Data_SliSpecification_Count_BadEventsFormulaFields,
  },
  goodEventsFormula: {
    wireName: "good_events_formula",
    kind: "object",
    fields: Sloresponse_Data_SliSpecification_Count_BadEventsFormulaFields,
  },
  queries: {
    wireName: "queries",
    kind: "list",
    fields: Sloresponse_Data_SliSpecification_Count_QueriesFields,
  },
  totalEventsFormula: {
    wireName: "total_events_formula",
    kind: "object",
    fields: Sloresponse_Data_SliSpecification_Count_BadEventsFormulaFields,
  },
};

const Sloresponse_Data_SliSpecification_TimeSlice_QueryFields: FieldMap = {
  formulas: {
    wireName: "formulas",
    kind: "list",
    fields: Sloresponse_Data_SliSpecification_Count_BadEventsFormulaFields,
  },
  queries: {
    wireName: "queries",
    kind: "list",
    fields: Sloresponse_Data_SliSpecification_Count_QueriesFields,
  },
};

const Sloresponse_Data_SliSpecification_TimeSliceFields: FieldMap = {
  comparator: "comparator",
  query: {
    wireName: "query",
    kind: "object",
    fields: Sloresponse_Data_SliSpecification_TimeSlice_QueryFields,
  },
  queryIntervalSeconds: "query_interval_seconds",
  threshold: "threshold",
};

const Sloresponse_Data_SliSpecificationFields: FieldMap = {
  count: {
    wireName: "count",
    kind: "object",
    fields: Sloresponse_Data_SliSpecification_CountFields,
  },
  timeSlice: {
    wireName: "time_slice",
    kind: "object",
    fields: Sloresponse_Data_SliSpecification_TimeSliceFields,
  },
};

const Sloresponse_Data_ThresholdsFields: FieldMap = {
  target: "target",
  targetDisplay: "target_display",
  timeframe: "timeframe",
  warning: "warning",
  warningDisplay: "warning_display",
};

export interface SloresponseConfig {
  /** A user-defined description of the service level objective. Always included in service level objective responses (but may be `null`). Optional in create/update requests. */
  description?: string | Computed<string>;
  /** A list of (up to 100) monitor groups that narrow the scope of a monitor service level objective. Included in service level objective responses if it is not empty. Optional in create/update requests for monitor service level objectives, but may only be used when then length of the `monitor_ids` field is one. */
  groups?: string[] | Computed<string[]>;
  /** A list of monitor IDs that defines the scope of a monitor service level objective. **Required if type is `monitor`**. */
  monitorIds?: number[] | Computed<number[]>;
  /** The name of the service level objective object. */
  name: string | Computed<string>;
  /** A count-based (metric) SLO query. This field is superseded by `sli_specification` but is retained for backwards compatibility. Note that Datadog only allows the sum by aggregator to be used because this will sum up all request counts instead of averaging them, or taking the max or min of all of those requests. */
  query?: Sloresponse_Data_Query | Computed<Sloresponse_Data_Query>;
  /** A generic SLI specification. This is used for time-slice and count-based (metric) SLOs only. */
  sliSpecification?: Sloresponse_Data_SliSpecification | Computed<Sloresponse_Data_SliSpecification>;
  /** A list of tags associated with this service level objective. Always included in service level objective responses (but may be empty). Optional in create/update requests. */
  tags?: string[] | Computed<string[]>;
  /** The target threshold such that when the service level indicator is above this threshold over the given timeframe, the objective is being met. */
  targetThreshold?: number | Computed<number>;
  /** The thresholds (timeframes and associated targets) for this service level objective object. */
  thresholds: Sloresponse_Data_Thresholds[] | Computed<Sloresponse_Data_Thresholds[]>;
  /** The SLO time window options. Note that "custom" is not a valid option for creating or updating SLOs. It is only used when querying SLO history over custom timeframes. */
  timeframe?: string | Computed<string>;
  /** The type of the service level objective. */
  type: string | Computed<string>;
  /** The optional warning threshold such that when the service level indicator is below this value for the given threshold, but above the target threshold, the objective appears in a "warning" state. This value must be greater than the target threshold. */
  warningThreshold?: number | Computed<number>;
  /** path parameter, not part of the API's own resource representation */
  sloId: string | Computed<string>;
}

export interface SloresponseAttrs {
  /** A service level objective object includes a service level indicator, thresholds for one or more timeframes, and metadata (`name`, `description`, `tags`, etc.). */
  data: Sloresponse_Data;
  /** A user-defined description of the service level objective. Always included in service level objective responses (but may be `null`). Optional in create/update requests. */
  description: string;
  /** An array of error messages. Each endpoint documents how/whether this field is used. */
  errors: string[];
  /** A list of (up to 100) monitor groups that narrow the scope of a monitor service level objective. Included in service level objective responses if it is not empty. Optional in create/update requests for monitor service level objectives, but may only be used when then length of the `monitor_ids` field is one. */
  groups: string[];
  /** A list of monitor IDs that defines the scope of a monitor service level objective. **Required if type is `monitor`**. */
  monitorIds: number[];
  /** The name of the service level objective object. */
  name: string;
  /** A count-based (metric) SLO query. This field is superseded by `sli_specification` but is retained for backwards compatibility. Note that Datadog only allows the sum by aggregator to be used because this will sum up all request counts instead of averaging them, or taking the max or min of all of those requests. */
  query: Sloresponse_Data_Query;
  /** A generic SLI specification. This is used for time-slice and count-based (metric) SLOs only. */
  sliSpecification: Sloresponse_Data_SliSpecification;
  /** A list of tags associated with this service level objective. Always included in service level objective responses (but may be empty). Optional in create/update requests. */
  tags: string[];
  /** The target threshold such that when the service level indicator is above this threshold over the given timeframe, the objective is being met. */
  targetThreshold: number;
  /** The thresholds (timeframes and associated targets) for this service level objective object. */
  thresholds: Sloresponse_Data_Thresholds[];
  /** The SLO time window options. Note that "custom" is not a valid option for creating or updating SLOs. It is only used when querying SLO history over custom timeframes. */
  timeframe: string;
  /** The type of the service level objective. */
  type: string;
  /** The optional warning threshold such that when the service level indicator is below this value for the given threshold, but above the target threshold, the objective appears in a "warning" state. This value must be greater than the target threshold. */
  warningThreshold: number;
  /** path parameter, not part of the API's own resource representation */
  sloId: string;
}

export const Sloresponse: ResourceBinding<SloresponseConfig, SloresponseAttrs> = {
  wireType: "datadog_sloresponse",
  fields: {
    description: "description",
    groups: "groups",
    monitorIds: "monitor_ids",
    name: "name",
    query: {
      wireName: "query",
      kind: "object",
      fields: Sloresponse_Data_QueryFields,
    },
    sliSpecification: {
      wireName: "sli_specification",
      kind: "object",
      fields: Sloresponse_Data_SliSpecificationFields,
    },
    tags: "tags",
    targetThreshold: "target_threshold",
    thresholds: {
      wireName: "thresholds",
      kind: "list",
      fields: Sloresponse_Data_ThresholdsFields,
    },
    timeframe: "timeframe",
    type: "type",
    warningThreshold: "warning_threshold",
    sloId: "slo_id",
  },
};
